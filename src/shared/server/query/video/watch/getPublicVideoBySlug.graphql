query GetPublicVideoBySlug($slug: String!) {
	getPublicVideoBySlug(slug: $slug) {
		access
		categories {
			id
		}
		createdAt
		description
		durationSeconds
		id
		ownerId
		path
		slug
		status
		thumbnailUrl
		title
		updatedAt
	}
}
